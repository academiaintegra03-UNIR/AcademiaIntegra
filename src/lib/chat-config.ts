/**
 * Shared between the Álex Route Handler (server) and the chat widget
 * (client). Counts only messages sent BY THE USER — not model replies.
 */
export const ORIENTATION_CHAT_MAX_USER_MESSAGES = 15;
export const ORIENTATION_CHAT_MAX_MESSAGE_LENGTH = 1000;
